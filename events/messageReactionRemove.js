module.exports = {
    name: `messageReactionRemove`,
    async execute(messageReaction, user) {
        // if (!client.reactionRoles.has(messageReaction.message.id)) return;
        // console.log(`${user.username} has removed a reaction from a message.`);
        // console.log(messageReaction);
    }
}